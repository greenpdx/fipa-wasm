[package]
name = "{{agent_name}}"
version = "0.1.0"
edition = "2021"
description = "FIPA WASM Agent (Minimal) - {{agent_name}}"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.36"

[profile.release]
opt-level = "s"
lto = true
strip = true
