[package]
name = "ping-pong-agent"
version = "0.1.0"
edition = "2021"
description = "FIPA Ping-Pong Example Agent - Echoes messages back to sender"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.36"

[profile.release]
opt-level = "s"
lto = true
strip = true
